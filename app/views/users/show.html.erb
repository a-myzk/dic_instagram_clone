<h1><%= @user.name %>のページ</h1>
<p>メールアドレス:<%= @user.email %></p>

<div>
  <% if @user.image.url != nil %>
    <%= image_tag @user.image.url %>
  <% else %>
    まだ登録されていません
  <% end %>
</div>

<%= link_to 'Favorite', favorites_path(@favorite) %>
<%= link_to 'Edit', edit_user_path(@user) %>
<%= link_to 'Back', pictures_path %>
